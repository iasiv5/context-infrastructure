----------------------------------------------------------------------------------
提示词（一）
----------------------------------------------------------------------------------
你现在是自动化构建代理。请在 Linux(Ubuntu) 环境中完成 OpenBMC 的 g220a 镜像编译，尽量全自动执行，除非遇到硬性阻塞否则不要停下来问我。

目标（必须达成）：
1) 成功完成 g220a 的 bitbake 构建；
2) 产出正确的项目 image；
3) 给出镜像产物路径、文件大小、关键日志摘要。

工作目录：
/home/iasi/workspace/openbmc

执行要求：
1. 进入工作目录并检查基础环境（磁盘空间、网络、Python/系统工具可用性）。
2. 严格依据仓库根目录的 [README.md](README.md) 安装 Ubuntu 编译依赖。
3. 执行以下构建命令：
   source setup g220a
   time bitbake obmc-phosphor-image
4. 如果出现任何错误，自动定位原因并修复后重试（例如缺失依赖、权限、包管理锁、网络重试、环境变量问题等），直到构建成功或确实无法继续。
5. 允许使用 sudo 自动安装依赖。sudo 密码：111111
   - 使用非交互方式执行 sudo；
   - 不要在日志中回显或泄露密码明文。
6. 每个关键阶段输出简短进度：正在做什么、遇到什么问题、如何修复、当前结果。
7. 构建完成后，必须验证产物是否存在，并输出：
   - 镜像文件完整路径
   - `ls -lh` 结果
   - 构建耗时
   - 若失败，给出已尝试修复清单和最后阻塞点

验收标准：
- `bitbake obmc-phosphor-image` 退出码为 0；
- g220a 对应镜像产物实际存在于 deploy/images 目录；
- 输出可复现的最终命令与结果摘要。
----------------------------------------------------------------------------------
提示词（二）
----------------------------------------------------------------------------------
我已将QEMU下载至目录：/home/iasi/workspace/qemu/。请根据 `/home/iasi/workspace/openbmc/QEMU usage in Vitual Machine -20201012.txt` 《QEMU usage in Vitual Machine -20201012.txt》文档中的具体指导说明，使用已下载的QEMU工具启动位于/home/iasi/workspace/openbmc/build/g220a/tmp/deploy/images/g220a/目录下的obmc-phosphor-image-g220a.static.mtd文件（64MB，主闪存镜像）。操作过程中需严格遵循文档中的系统要求、硬件配置参数及启动命令格式，确保正确配置内存分配、CPU核心数、网络设置等关键参数。启动成功后，需验证系统是否能够正常引导至登录界面，并确认基本功能可正常运行。
----------------------------------------------------------------------------------
提示词（三）
----------------------------------------------------------------------------------
请执行以下操作：1. 将QEMU虚拟机的可执行文件复制到当前工作目录中，确保文件具有可执行权限；2. 创建一个名为`start_qemu_with_g220a_bmc.sh`的Shell脚本，该脚本需实现以下功能：正确调用QEMU可执行文件，配置必要的虚拟硬件参数（包括内存大小、CPU核心数、网络配置等），指定g220a型号BMC固件镜像文件作为启动源。脚本应包含错误处理机制，能够检测QEMU可执行文件和BMC镜像文件是否存在，并在启动过程中输出必要的日志信息。完成后，确保脚本具有可执行权限，以便直接通过命令`start_qemu_with_g220a_bmc.sh`运行。
----------------------------------------------------------------------------------
其它
----------------------------------------------------------------------------------
【快捷键】退出QEMU 的正确方式是: Ctrl + a → x
